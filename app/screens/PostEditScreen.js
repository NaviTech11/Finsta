import React from 'react';
import { View, StyleSheet } from 'react-native';

function PostEdit(props) {
  return (
    <View style={styles.container}>{'PostEditScreen'}</View>
  );
}

const styles = StyleSheet.create({
  container: {}
});

export default PostEdit;